import type { Task } from '@/store'

export const loadListProjectTasks = () => {
  // wip
  return Promise.resolve({
    data: [] as Task[],
  })
}

export const loadSmartProjectTasks = (smartProjectId: string) => {
  // wip
  return Promise.resolve({
    data: [] as Task[],
  })
}

export const loadConcludedProjectTasks = async () => {
  // wip

  return Promise.resolve({
    data: [] as Task[],
  })
}
